@model SearchVM<Team>
@{
    ViewBag.Title = "Manage Teams";
}

<div class="mt-3">
    <form method="get" asp-action="Index" asp-controller="Team">
        <div class="input-group">
            <input type="text" class="form-control" placeholder="Search Teams..." name="searchString" asp-for="Search">
            <button class="btn btn-dark" type="submit">
                <i class="fa-solid fa-magnifying-glass"></i>
            </button>
        </div>
    </form>
</div>

@if (Model.Search != String.Empty)
{
    <div>
        <p>@Model.Items.Count() results for @Model.Search</p>
    </div>
}
@if (Model.Items.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Name</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (Team team in Model.Items)
            {
                <tr>
                    <td class="">@team.Name</td>
                    <td>
                        <a asp-action="Manage" asp-route-id="@team.TeamId" class="btn btn-primary">Manage</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
